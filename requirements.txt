# Ingestion
feedparser==6.0.12
requests==2.33.1
tqdm==4.66.2

# Database
sqlalchemy==2.0.40
alembic==1.15.2

# Utilities
python-dotenv==1.2.2
loguru==0.7.3

# Optional - use if podcast has no direct MP3 URLs in RSS
# yt-dlp==2024.3.10

# Transcription (phase 2)
faster-whisper==1.1.1
# PyTorch — install separately via:
# pip install torch==2.10.0 torchaudio==2.10.0 torchvision==0.25.0 --index-url https://download.pytorch.org/whl/cu128

# Diarization (phase 3)
pyannote.audio==4.0.4
soundfile==0.13.1

# Processing (phase 4)
llama-index-core==0.14.21
llama-index-embeddings-huggingface==0.7.0
tiktoken==0.12.0

# Vector DB (phase 5)
chromadb==1.5.8
sentence-transformers==4.1.0

# LLM (phase 6)
groq==1.2.0

# Streamlit (phase 7)
streamlit==1.57.0

# Testing
pytest==8.3.4